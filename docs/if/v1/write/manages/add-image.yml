post:
  summary: 画像追加
  description: 画像を画像一覧に追加する
  tags:
    - "manages"
  parameters:
    - in: body
      name: path
      description: 画像パスを入力する（rootディレクトリ省く）
      required: true
      schema:
        type: string
  responses:
    "200":
      description: "成功。更新が正常に行われました。"
    "400":
      description: Bad request
